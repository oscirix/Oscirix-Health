export interface Patient { id: string; hcu_number?: string; document_id?: string; full_name: string; sex?: 'male' | 'female' | 'unknown'; birth_date?: string; blood_type?: string; whatsapp_phone?: string; email?: string; }
export interface PatientAlert { id: string; patient_id: string; alert_type: 'allergy' | 'medical_condition' | 'habit' | 'risk' | 'note'; severity: 'low' | 'moderate' | 'high' | 'critical'; title: string; description?: string; is_active: boolean; }
export interface ClinicalRecord { id: string; patient_id: string; appointment_id?: string; record_type: 'clinic_visit' | 'external_intervention' | 'surgery' | 'emergency' | 'follow_up' | 'other'; origin: 'internal' | 'external' | 'imported'; record_date: string; title?: string; status: 'draft' | 'signed' | 'closed' | 'amended'; }
export interface Prescription { id: string; clinical_record_id: string; patient_id: string; status: 'draft' | 'active' | 'suspended' | 'completed' | 'cancelled'; prescribed_at?: string; }
export interface ExamRequest { id: string; clinical_record_id: string; patient_id: string; exam_category: string; exam_type: string; status: 'requested' | 'pending' | 'completed' | 'cancelled'; }
export interface PatientDocument { id: string; patient_id: string; clinical_record_id?: string; document_category: string; title: string; file_name: string; signed_status: 'not_required' | 'pending' | 'signed'; }
